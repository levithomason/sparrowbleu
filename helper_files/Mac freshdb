# dropdb
## run(dropdb --if-exists sbp)

# create the db
## run(createdb sbp)

# drop user if exists
## run(psql -d sbp -c "DROP ROLE IF EXISTS postgres")

# create database super user
## run(psql -d sbp -c "CREATE ROLE postgres PASSWORD 'admin' SUPERUSER CREATEROLE CREATEDB LOGIN;")
